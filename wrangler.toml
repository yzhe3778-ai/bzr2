name = "dream-comment-generator"
compatibility_date = "2024-01-01"
type = "javascript"

# Serve static files from the built Vite output
site = { bucket = "./dist" }

# Route API requests to the worker script
[[routes]]
pattern = "/api/*"
script = "worker"
